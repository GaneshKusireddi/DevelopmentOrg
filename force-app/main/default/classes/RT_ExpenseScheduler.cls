global class RT_ExpenseScheduler implements Schedulable{
global void execute (SchedulableContext SC){
    /*
RT_ExpenseToUpdateDueDateStatus updateduedates = new RT_ExpenseToUpdateDueDateStatus ();
database.executeBatch(updateduedates);
  */
}
}